# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with: # هذا السطر يبدأ بمسافة بادئة قدرها 4 مسافات تحت "- uses: FirebaseExtended/action-hosting-deploy@v0"
          repoToken: '${{ secrets.GITHUB_TOKEN }}' # هذا السطر يبدأ بمسافة بادئة قدرها 6 مسافات تحت "with:"
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_QYEM_SIGNATURE }}' # 6 مسافات
          channelId: live # 6 مسافات
          projectId: qyem-signature # 6 مسافات
          entryPoint: . # 6 مسافات
